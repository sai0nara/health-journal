1. ~~If on Export tab user pick Zip Archive and after it rotates phone, pick will return to default. Every rotation of the phone resets the pick.~~ **FIXED** - Export tab state now uses `rememberSaveable` so the format pick (and date range, tab) survive configuration changes / rotation.
2. ~~User can't srcroll down to the button 'Generate export' to confirm, export~~ **FIXED** - Export tab content is now vertically scrollable so the 'Generate Export' button is always reachable on small screens.
3. ~~'Zip (Raw Data & Media)' in to too narrow column so on narrow screen it looks like column of letters like this~~ **FIXED** - Format options are now laid out as separate full-width rows so the label wraps naturally instead of collapsing into single-letter columns.
4. ~~On attempt to restore backup user gets an Error 'Missing backup.json in backup'. The file 'data.json' is present on manual check~~ **FIXED** - Export tab's ZIP now routes through the full backup use case, which writes the required `backup.json` manifest, so exported archives are restorable.